export default class Staffmanagement {
    id: number;
    realid: number;
    type: number; // 0 - everything signed and unsigned.
    head: number; // head positions assigned to structures where they are heading
}