export default class Usersettings {
    updatePositioncompact: number; // 0 - no/ignore, 1 - true
    positioncompactValue: number; // if update, new value
    updateDate: number; //0 - no/ignore, 1 - true
    dateValue: Date; 
    updateSidebarDisplay: number;
    sidebarDisplayValue: number;
}
